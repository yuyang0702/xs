# Architecture residual convergence (P0-P6)

## Change contract

- Requested outcome: finish the remaining project-wide architecture convergence work in one authorized P0-P6 implementation without changing novel-business behavior.
- Original material MUST requirements: no untreated legacy residual from the audited error, orchestration, encoding, or startup-lifecycle families; preserve every existing novel, formal manuscript, model binding, Skill, run history, checkpoint, Canon, and StoryState authority.
- Scope classification: open_world for shared failure handling and orchestration convergence; closed_world for the identified mojibake and deprecated FastAPI startup hook.
- Operational definition: one typed/redacted failure projection, one route/runtime boundary, one formal-promotion authority, one startup lifecycle, and coordinator modules that delegate to existing business validators. Legacy paths cease to be authoritative only after parity evidence.
- Forbidden narrowing: no provider-name, error-string, route, novel, genre, fixture, or single-endpoint branches; no line-count-only refactor; no weakening of planning, causal, manifest, semantic, quality, originality, maintenance, or promotion gates.
- Authorization: implementation and offline verification. No paid API call, service restart, Git commit/push, destructive migration, or live formal-data mutation.
- Current behavior and evidence: the clean baseline at commit `5a51735` passes 153 focused/runtime/transaction/production-length tests; all literal generated-artifact contracts are registered; residual raw exception projections, two reachable mojibake messages, an unreachable style fallback, an oversized workflow orchestrator, and deprecated startup registration remain.
- Allowed changes: shared safe failure projection and API helper; migration of unsafe API/persistence sinks; deterministic encoding cleanup; behavior-preserving coordinator extraction; FastAPI lifespan migration; deletion of proven-unreachable duplicate paths; tests and maintenance documentation.
- Protected unchanged behavior: story ownership, character/knowledge state, causal order, relationship progression, promises/payoffs, ending authority, quality outcomes, checkpoint reuse, model-role selection, retry/fallback order, and formal-promotion eligibility.
- Authority impact: formal manuscript/current candidate/protected best/StoryState/outline/causal/manifest/receipts remain read-only during development tests; UI/API error envelopes change additively; persisted exception detail becomes hash-only; startup and workflow orchestration remain behind existing public entry points.
- Selected approach: typed failure envelope plus safe projections; modular-monolith coordinator facades; `wrap -> shadow/parity -> switch -> delete`; FastAPI lifespan; hash-bound regression and current-project isolated replay.
- Rejected alternatives: global `str(exc)` replacement without taxonomy; a second task/runtime framework; rewriting business validators; big-bang workflow rewrite; deleting legacy readers by search alone; truncating prompts or manuscripts to pass tests.
- Rollback path: each phase is independently revertible; old workflow entry methods remain until parity; mismatches preserve the last accepted artifact and old authority; no live project is an experimental output.
- Focused tests: failure-boundary unit/API/persistence sentinels; coordinator parity; lifespan once-only recovery; encoding scanner; legacy reachability.
- Related tests: tasks/supervisor, contract runtime, generated artifacts, project transactions, API, workflows, learning/distillation, planning/manifest/draft/quality.
- Full-suite requirement: complete `pytest -q`, strict change gate, clean-room single-agent report, current-project isolated replay, and 13K/20K/30K complete short-fiction flows.
- Historical incident families checked: provider transport, credentials/capability, input capacity, output truncation, parser ambiguity, ownership/evidence, semantic regression, stale authority, partial/conflicting checkpoints, merge conflicts, quality regression, promotion interruption, cancellation and resume.
- Projected sibling and downstream risks: initialization, planning, causal chain, execution manifest, drafting, split/merge, polish, targeted/manual revision, AI issue refresh, final review, maintenance, and formal promotion.
- Model-output boundary changed: no. The existing generated-artifact registry and domain validators remain authoritative.
- Unknown-variant behavior: unknown or ambiguous model representations remain in the existing typed repair/fallback ladder; this refactor adds no representation aliases.
- Resolution target: `systemically_resolved`, contingent on all P6 evidence.
- Why previous tests missed the residuals: prior gates emphasized successful narrative production and promotion safety, not a repository-wide no-raw-exception-sink invariant, source-encoding reachability, file-level orchestration convergence, or lifespan registration.
- Unresolved user decisions: none for implementation. Paid canary, restart, commit, and push remain separately authorized.

## Convergence budget

1. Inventory every raw exception API/persistence sink and replace it with the single safe projection.
2. Preserve `WorkflowService` as the public facade while extracting orchestration-only units; do not copy domain validators.
3. Replace startup decorators with one lifespan entry and prove idempotent recovery.
4. Delete only paths with call-graph, compatibility, and regression evidence.
5. Treat code-size reduction as a diagnostic, never an acceptance condition.

