# Narrative Participant Identity Compatibility V6

## Change contract

- Requested outcome: fix the production failure in which a valid first-person narrator realization is rejected as a missing formal-event participant, while remaining compatible across novels, narrator identities, languages, viewpoints, providers, and previously accepted planning representations.
- Original material MUST requirements: the solution must not hard-code the current novel, `花穗`, `我`, one event ID, or one provider; it must preserve narrative logic and work across novels.
- Scope classification: `open_world`.
- Operational definition: Runtime validates canonical participant identity against a project-owned realization contract. Explicit canonical names retain their current meaning. A ready first-person narrator may additionally be realized by that contract's self-reference in narrative voice outside unowned quoted speech. Third-person and missing/ambiguous narrator contracts retain literal canonical-name behavior. Unknown or ambiguous references remain blocking instead of being guessed.
- Forbidden narrowing: no character-name, event-ID, run-ID, genre, provider, Chinese-only, or exact-sentence branch; no rule that treats every `我`/`I` anywhere as the narrator; no disabling participant coverage; no model rewrite merely to insert a forbidden third-person narrator name.
- Authorization: implementation and tests. No paid API calls, restart, formal-manuscript writes, Git commit, or Git push are authorized by this request.
- Current behavior and evidence: production run `5bb4b703f6ff471694a32fd886c3a041` reduced 29 event-body ownership issues to zero, then rejected two complete segment-5 rebuilds because `planning_event_obligation_issues` used literal `name not in event_text`. The project narrative contract is `first_person_limited`, narrator `花穗`, self-reference `我`; both candidates contain narrator-owned `我` actions outside dialogue and correctly avoid third-person narrator drift.
- Allowed changes: narrative-contract projection, deterministic participant-realization validation, planning obligation checklists, recovery prompts/diagnostics, production incident catalog, tests, fixtures, and maintenance documentation.
- Protected unchanged behavior: formal outlines and manuscripts, StoryState, project files, event ownership/order, non-narrator participant coverage, third-person validation, quote ambiguity safety, provider routing, capacity policy, recovery budgets, checkpoints, and unaffected plan segments.
- Authority impact: the project narrative contract remains the sole authority for narrator identity. Planning obligation validation may recognize an additional contract-bound textual realization; it gains no authority to infer a new character or rewrite prose.
- Selected approach: typed canonical identity-realization contracts, Unicode normalization, deterministic quote-aware narrative-voice projection, and invariant-based tests. This follows entity normalization/alias resolution while keeping authorization hash-bound to the existing narrative contract.
- Rejected alternatives: literal alias lists, global coreference guesses, accepting every first-person token including another speaker's dialogue, excluding the narrator from required participants, or forcing a model to spell the narrator name in first-person prose.
- Rollback path: remove realization projection from obligation checklists; explicit-name behavior is otherwise unchanged. Existing checkpoints and artifacts contain no required schema migration.
- Focused tests: production-shaped first-person evidence, quoted-speaker negative case, custom narrator/self-reference, third-person unchanged behavior, normalization and idempotence.
- Related tests: narrative-contract, planning adaptation, planning recovery/workflow, production incidents, causal-chain continuation, cross-genre rules, and complete suite.
- Full-suite requirement: `.venv/Scripts/python.exe -X utf8 -m pytest -q` before completion; no live provider calls.
- Historical incident families checked: `planning.event_obligation_incomplete`, `planning.structure_drift`, `narrative.first_person_contract_missing`, `planning.event_body_integrity`, `planning.presentation_normalized_ownership_revealed`, and downstream causal-chain/manifest/draft integrity families.
- Projected sibling and downstream risks: dialogue pronouns falsely claiming narrator identity; third-person projects weakening; custom or multilingual self-references; stale/ambiguous contracts; local repair appending redundant formal excerpts; repair packets using a different validator; recovered plans failing causal-chain continuation.
- Model-output variants and stable invariants: explicit canonical name, Chinese/English/custom self-reference, subject-bearing and subject-elliptic continuation after a proved narrator reference, Markdown paragraph/list/card/table-normalized forms, quoted-only negative cases, and unrelated missing participants. Stable oracles are canonical identity, event ownership, quote ownership, unchanged sibling hashes, issue identity, and downstream event coverage.
- Unknown-variant behavior: only Runtime-owned canonical names and a ready narrator contract's exact self-reference are accepted. Unknown aliases, quoted-only references, ambiguous contracts, or unproved subject ellipsis remain blocking and enter bounded recovery.
- Requirement-to-code/test/evidence traceability: canonical realization is implemented in `narrative_contract.py` and consumed by `planning_adaptation.py`; all planning checklist construction paths in `workflows.py` bind it; production and cross-novel tests live in `tests/test_planning_adaptation.py`, `tests/test_planning_adaptation_workflow.py`, and `tests/test_production_incidents.py`.
- Resolution status before implementation: `unresolved`.
- Resolution status after implementation: `systemically_resolved`. The production fixture, six cross-novel/language realizations, six presentation topologies, invalid/ambiguous cases, planning workflow continuation, causal-chain boundary, 512 related regressions, and the strict L3 forward-risk gate pass without a live provider call.
- Why previous tests missed the production shape: all complete-event fixtures repeated the narrator's canonical name, while first-person contract tests were confined to draft/polish prose and never crossed the planning participant-obligation validator.
- Forward-risk report: `C:\小说\.codex-task-baselines\narrative-identity-compat-forward-risk.json`.
- Unresolved user decisions: none.
